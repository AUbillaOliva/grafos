// Theme
export const SWITCH_THEME = "SWITCH_THEME";
const THEME_TYPES = {
    switch: SWITCH_THEME,
};

// Errors
export const ERROR = "ERROR";
const ERROR_TYPES = {
    error: ERROR,
}

export {
    THEME_TYPES,
    ERROR_TYPES,
};
